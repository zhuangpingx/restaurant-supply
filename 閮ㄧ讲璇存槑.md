# 供应链协同系统 - 部署说明

## 第一步：安装 Node.js
访问 https://nodejs.org 下载 LTS 版本安装

## 第二步：安装项目依赖
在项目文件夹内，按住 Shift + 右键 → 在此处打开 PowerShell
输入命令：
```
npm install
```

## 第三步：配置 Supabase
1. 访问 https://supabase.com 注册账号
2. 新建项目（Region 选 Tokyo）
3. 进入 SQL Editor，执行 supabase建库.sql 文件内的所有SQL
4. 进入 Storage，新建两个 Bucket：
   - delivery-photos（私有）
   - payment-proofs（私有）
5. 进入 Settings → API，复制三个值：
   - Project URL
   - anon public key
   - service_role key

## 第四步：配置环境变量
在项目根目录新建文件 .env.local，填入：
```
NEXT_PUBLIC_SUPABASE_URL=你的ProjectURL
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的anonKey
SUPABASE_SERVICE_ROLE_KEY=你的serviceRoleKey
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

## 第五步：本地测试
```
npm run dev
```
打开 http://localhost:3000 看到登录页说明成功

## 第六步：上传到 GitHub
1. 访问 https://github.com 注册账号
2. 新建仓库（New Repository），名字叫 restaurant-supply
3. 在项目文件夹执行：
```
git init
git add .
git commit -m "first commit"
git remote add origin https://github.com/你的用户名/restaurant-supply.git
git push -u origin main
```

## 第七步：部署到 Vercel
1. 访问 https://vercel.com 用 GitHub 账号登录
2. 点 Add New Project
3. 选择 restaurant-supply 仓库
4. 展开 Environment Variables，添加和 .env.local 一样的 4 个变量
   （APP_URL 改为 https://你的项目名.vercel.app）
5. 点 Deploy，等待约 2 分钟
6. 部署完成，获得网址即可使用

## 遇到问题
把报错信息发给 Claude，帮你解决。
